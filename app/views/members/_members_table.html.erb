<% members.each do |member| %>
  <tr>
    <td><%= check_box_tag 'member_ids[]', member.id %></td>
    <td><%= member.name %></td>
    <td><%= member.email %></td>
    <td>
      <%= link_to '編集', edit_member_path(member), class: 'btn btn-primary' %>
    </td>
  </tr>
<% end %>
